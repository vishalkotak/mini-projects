### #[derive(Debug)]
struct ...

- Whole statement defines attributes for the struct giving the rust compiler some extra instructions
- Called the 'derive' attribute. Specifies which traits to automatically implement for this struct.
- Called the 'Debug' trait.


